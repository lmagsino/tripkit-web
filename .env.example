# Rails API Configuration
NEXT_PUBLIC_API_URL=http://localhost:3000/api
# Note: NEXT_PUBLIC_ prefix makes it available in browser
# Variables without this prefix are server-only (more secure for secrets)
